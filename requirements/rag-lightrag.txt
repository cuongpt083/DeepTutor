# ============================================
# DeepTutor RAG LightRAG — Dependencies
# ============================================
# Mirrors pyproject.toml `[project.optional-dependencies].rag-lightrag`.
# Keep in sync with pyproject.toml when adding/updating dependencies.
#
# Preferred install (from a source clone): pip install -e ".[rag-lightrag]"
# Use this file for Docker/CI when pyproject.toml/source code aren't yet available.

raganything>=1.2.5,<2.0.0; python_version < "3.14"
